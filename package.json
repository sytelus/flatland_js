{
  "name": "flatland-js",
  "version": "0.0.0",
  "private": true,
  "description": "A self-contained browser demo of a 2D world viewed through a 1D visual field.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=18"
  }
}
